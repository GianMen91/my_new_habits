import 'package:flutter/material.dart';

// General Padding
const defaultPadding = 20.0;

// Background and UI element colors
const backgroundColor =
    Color(0xFFFFFBF5); // Light background color for main screen
const selectedColor = Color(0xFFFFC7AD); // Accent color for selected items
const boldTextColor = Color(0xFFF8AB86); // Bold text color for emphasis

// UI component colors
const touchedBarColor = Color(0xFFA2E0D5); // Color for bar when touched
const bottomNavigationBarColor =
    Color(0xFFFFFFFF); // Color for bottom navigation bar
